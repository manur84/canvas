<UserControl x:Class="LayoutDesigner.Controls.ColorInputControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="32" d:DesignWidth="300">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="32"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <!-- Color Preview -->
        <Border Grid.Column="0"
                Background="{Binding ColorBrush, RelativeSource={RelativeSource AncestorType=UserControl}}"
                BorderBrush="{StaticResource BorderBrush}"
                BorderThickness="1"
                CornerRadius="3"
                Margin="0,0,4,0"
                ToolTip="Current color preview">
            <!-- Checkered background for transparency indication -->
            <Border.Style>
                <Style TargetType="Border">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding ShowTransparencyPattern, RelativeSource={RelativeSource AncestorType=UserControl}}" Value="True">
                            <Setter Property="Background">
                                <Setter.Value>
                                    <DrawingBrush Viewport="0,0,8,8" ViewportUnits="Absolute" TileMode="Tile">
                                        <DrawingBrush.Drawing>
                                            <DrawingGroup>
                                                <GeometryDrawing Brush="#FFCCCCCC">
                                                    <GeometryDrawing.Geometry>
                                                        <RectangleGeometry Rect="0,0,8,8"/>
                                                    </GeometryDrawing.Geometry>
                                                </GeometryDrawing>
                                                <GeometryDrawing Brush="#FFFFFFFF">
                                                    <GeometryDrawing.Geometry>
                                                        <GeometryGroup>
                                                            <RectangleGeometry Rect="0,0,4,4"/>
                                                            <RectangleGeometry Rect="4,4,4,4"/>
                                                        </GeometryGroup>
                                                    </GeometryDrawing.Geometry>
                                                </GeometryDrawing>
                                            </DrawingGroup>
                                        </DrawingBrush.Drawing>
                                    </DrawingBrush>
                                </Setter.Value>
                            </Setter>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Border.Style>

            <!-- Actual color overlay -->
            <Rectangle Fill="{Binding ColorBrush, RelativeSource={RelativeSource AncestorType=UserControl}}"/>
        </Border>

        <!-- Hex TextBox -->
        <TextBox Grid.Column="1"
                 x:Name="ColorTextBox"
                 Text="{Binding ColorValue, RelativeSource={RelativeSource AncestorType=UserControl}, UpdateSourceTrigger=PropertyChanged}"
                 Style="{StaticResource ModernTextBox}"
                 FontFamily="Consolas"
                 ToolTip="Enter hex color (#RRGGBB or #AARRGGBB)"/>

        <!-- Pick Color Button -->
        <Button Grid.Column="2"
                Content="..."
                Click="OnPickColorClick"
                Style="{StaticResource IconButton}"
                Width="32"
                Height="28"
                Margin="4,0,0,0"
                ToolTip="Open color picker dialog"/>
    </Grid>
</UserControl>
