<UserControl x:Class="LayoutDesigner.Views.AssetManagerPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="250"
             Background="{StaticResource BackgroundBrush}">

    <Border BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,1,0">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Border Grid.Row="0" Background="{StaticResource PrimaryBrush}" Padding="12,8">
                <TextBlock Text="Asset Manager"
                          FontSize="16"
                          FontWeight="Bold"
                          Foreground="White"/>
            </Border>

            <!-- Actions -->
            <StackPanel Grid.Row="1" Margin="12,8">
                <Button Content="Import Image..."
                       Click="OnImportClick"
                       Style="{StaticResource ModernButton}"
                       Height="32"/>
                <TextBlock Text="{Binding ElementName=AssetsList, Path=Items.Count, StringFormat='{}{0} assets'}"
                          Margin="0,8,0,0"
                          FontSize="11"
                          Foreground="{StaticResource SecondaryBrush}"
                          HorizontalAlignment="Center"/>
            </StackPanel>

            <!-- Asset Grid -->
            <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
                <ItemsControl x:Name="AssetsList" Margin="8">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel Orientation="Horizontal"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>

                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Background="White"
                                   BorderBrush="{StaticResource BorderBrush}"
                                   BorderThickness="1"
                                   Width="100"
                                   Height="120"
                                   Margin="4"
                                   Cursor="Hand"
                                   MouseLeftButtonDown="OnAssetClick"
                                   ToolTip="{Binding FileName}">
                                <Border.ContextMenu>
                                    <ContextMenu>
                                        <MenuItem Header="Insert to Canvas" Click="OnInsertAssetClick"/>
                                        <MenuItem Header="Delete" Click="OnDeleteAssetClick"/>
                                    </ContextMenu>
                                </Border.ContextMenu>

                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Style.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
                                                <Setter Property="BorderThickness" Value="2"/>
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>

                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="*"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>

                                    <!-- Thumbnail -->
                                    <Border Grid.Row="0" Margin="4">
                                        <!-- Checkered background for transparency -->
                                        <Border.Background>
                                            <DrawingBrush Viewport="0,0,8,8" ViewportUnits="Absolute" TileMode="Tile">
                                                <DrawingBrush.Drawing>
                                                    <DrawingGroup>
                                                        <GeometryDrawing Brush="#FFEEEEEE">
                                                            <GeometryDrawing.Geometry>
                                                                <RectangleGeometry Rect="0,0,8,8"/>
                                                            </GeometryDrawing.Geometry>
                                                        </GeometryDrawing>
                                                        <GeometryDrawing Brush="#FFFFFFFF">
                                                            <GeometryDrawing.Geometry>
                                                                <GeometryGroup>
                                                                    <RectangleGeometry Rect="0,0,4,4"/>
                                                                    <RectangleGeometry Rect="4,4,4,4"/>
                                                                </GeometryGroup>
                                                            </GeometryDrawing.Geometry>
                                                        </GeometryDrawing>
                                                    </DrawingGroup>
                                                </DrawingBrush.Drawing>
                                            </DrawingBrush>
                                        </Border.Background>

                                        <Image Source="{Binding FullPath, Converter={StaticResource ImagePathConverter}}"
                                              Stretch="Uniform"
                                              RenderOptions.BitmapScalingMode="HighQuality"/>
                                    </Border>

                                    <!-- File Info -->
                                    <Border Grid.Row="1" Background="#F0F0F0" Padding="4,2">
                                        <StackPanel>
                                            <TextBlock Text="{Binding FileName}"
                                                     FontSize="9"
                                                     TextTrimming="CharacterEllipsis"
                                                     HorizontalAlignment="Center"
                                                     FontWeight="SemiBold"/>
                                            <TextBlock Text="{Binding FileSizeFormatted}"
                                                     FontSize="8"
                                                     Foreground="{StaticResource SecondaryBrush}"
                                                     HorizontalAlignment="Center"/>
                                        </StackPanel>
                                    </Border>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Grid>
    </Border>
</UserControl>
